# projrvt

Minimal Python project scaffold for `projrvt`.

Quick start:

- Create a virtual environment: `python -m venv .venv`
- Install dev deps: `pip install -r requirements.txt`
- Run tests: `pytest`
